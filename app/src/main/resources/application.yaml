datasource:
  default: ${APP_ENV}
  development:
    migration:
      run: true
    username: ""
    password: ""
    url: "jdbc:h2:./database"
  production:
    platform: postgres
    ddlMode: dropCreate
    migration:
      run: true
    username: ${PGUSER}
    password: ${PGPASSWORD}
    url: ${DATABASE_URL}

